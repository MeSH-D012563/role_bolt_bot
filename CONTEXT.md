# Контекст проекта

## Что это
Telegram‑бот на Python (`aiogram 3`) с внутриигровой валютой, тремя мини‑играми, магазином и системой уникальных титулов.

## Основной функционал
- `/start` — регистрация профиля.
- `/help` — справка по командам в ЛС.
- `/profile` — профиль в ЛС.
- `/get_cash` — ежедневный бонус.
- `/pay <сумма>` — перевод ответом на сообщение.
- `/shop`, `/help_shop`, `/shop_help`, `/buy <item_id>` — магазин.
- `/set_title <title_id|none>` — выбор активного титула.
- `/sell_title <цена>`, `/sell_title_now`, `/gift_title` — оборот титулов между игроками и мгновенный выкуп ботом с подтверждением в ЛС.
- `/slot <ставка>` — слот.
- `/dice <ставка>` — дуэль в группе ответом на сообщение.
- `/basket <ставка>`, `/basket_continue`, `/basket_cashout` — баскетбол.
- `/top_balance`, `/top_lost` — рейтинги.

## Архитектура
- `aiogram 3`
- SQLite через `aiosqlite`
- основная конфигурация в `bot/config.py`
- игровые вычисления в `bot/services/games.py`
- логика титулов и магазина в `bot/services/shop.py`

## Хранилища состояния
- `data/bot.db` — пользователи, экономика, дуэли, баскет‑банки, защиты, наказания, владение титулами и продажи титулов
- отдельного состояния Telegram‑сессии нет, достаточно `BOT_TOKEN`

## Текущие дефолтные параметры
- стартовый баланс: `200` `bolt_coin`
- лимиты ставки: `1` – `1_000_000`
- задержка анимаций: `2.0` сек
- rate‑limit команд/кнопок: `1.5` сек
- ежедневный бонус: `75` `bolt_coin` раз в `12` часов
- дуэль: TTL `120` сек
- налог на титулы: `2.5%` раз в `24` часа при `2+` титулах
- льготный период по долгу: `24` часа

## Текущий баланс игр
### 🎰 Слот
- исход Telegram `1..64` раскладывается в 3 символа
- тройки:
  - `7️⃣` → `x6.2`
  - `BAR` → `x3.8`
  - `🍒` → `x2.6`
  - `🍋` → `x1.6`
- пары:
  - `7️⃣` → `x1.55`
  - `BAR` → `x1.35`
  - `🍒` → `x1.15`
  - `🍋` → `x1.0`
- прочие исходы проигрышные

### 🏀 Баскетбол
- успешные значения: `4`, `5`
- множитель банка за попадание: `x2.35`
- игрок сам решает, продолжать серию или забирать банк

### 🎲 Дуэль
- бот не берёт комиссию
- ничья возвращает ставки
- тайм‑аут автоматически возвращает ставку инициатору
- титулы не влияют на PvP

## Система титулов
Проблема старой версии: все титулы давали один и тот же линейный бонус к выигрышам, из‑за чего экономика ломалась, а сами титулы были малоразличимы.

Текущая модель использует `TitleEffect` в `bot/config.py`:
- `slot_bonus_bp`
- `basket_bonus_bp`
- `loss_refund_bp`
- `daily_bonus_amount`
- `protection_discount_bp`

Это позволяет делать титулы ролевыми и одновременно выстраивать их по силе относительно цены:
- экономические
- риск‑менеджмент
- слот‑специалисты
- баскет‑специалисты
- универсальные гибриды

Дополнительно действует анти‑монополия:
- первый титул бесплатен по содержанию
- при `2+` титулах бот считает ежедневный налог от полной стоимости всех титулов игрока
- если денег не хватает, создаётся долг на `24` часа
- пока долг активен, эффекты титулов отключены
- если долг не погашен вовремя, конфискуется один неактивный титул
- активный титул можно мгновенно продать боту за `90%` магазинной цены

## Реализованные улучшения
- транзакции для денежных операций
- авто‑отмена дуэлей по TTL
- корректная обработка ничьей
- блокировка двойных ходов в баскетболе
- частичный возврат ставки по эффектам титулов
- персональные скидки на защиты по активному титулу
- бонус к `/get_cash` по активному титулу
- фоновая система налога на титулы с долгом и конфискацией

## Полезные файлы
- `bot/config.py` — экономика, магазин, титулы, коэффициенты
- `bot/handlers/games.py` — слот, дуэль, баскетбол
- `bot/handlers/shop.py` — магазин и покупки
- `bot/handlers/profile.py` — вывод титулов и защит
- `bot/services/shop.py` — описания и расчёт эффектов титулов
- `bot/services/games.py` — расчёт выплат и возвратов

## Запуск
1. `pip install -r requirements.txt`
2. заполнить `.env`
3. `python -m bot`
