# Контекст проекта

## Что это
Телеграм‑бот на Python (aiogram 3) с тремя мини‑играми на внутреннюю валюту и рейтингами.

## Основной функционал
- /start — создание профиля. Пока пользователь не сделал /start, бот отвечает: «Для начала нужно создать профиль: /start».
- /help — справка по командам (только ЛС).
- /help_shop — справка по магазину (только ЛС).
- /shop_help — алиас справки по магазину.
- /shop — список товаров (только ЛС).
- /buy <item_id> [@юзер] — покупка товара.
- /set_title <title_id|none> — выбор титула.
- 🎰 /slot <ставка> — слот‑автомат на emoji‑dice.
- 🎲 /dice <ставка> <@юзер> или reply — дуэль с подтверждением через кнопку.
- 🏀 /basket <ставка> — баскетбол с банком (можно продолжать или забрать).
- /basket_continue — продолжить баскетбол.
- /basket_cashout — забрать банк.
- /profile — профиль только в ЛС.
- /top_balance — топ по балансу.
- /top_lost — топ по проигрышам.

## Архитектура
- Асинхронный бот: `aiogram 3`.
- База данных: SQLite через `aiosqlite`.
- Конфигурация вынесена в `bot/config.py` (класс `Settings`).
- Все ключевые параметры (баланс, валюта, коэффициенты, тайм‑ауты, задержки) — в `Settings`.

## Хранилища состояния
- Вся экономика и профили: `data/bot.db` (SQLite).
- Сессия Telegram бота не хранится отдельно — достаточно `BOT_TOKEN`.

## Важные правила, согласованные с пользователем
- Только `/start` для регистрации.
- Игры — на внутреннюю валюту.
- Нужна задержка между броском и результатом (анимация).
- Профиль только в личных сообщениях.
- Дополнительные улучшения допустимы и приветствуются.

## Реализованные улучшения
- Тайм‑аут дуэли (120 сек) с возвратом ставки инициатору.
- Ничья в дуэли возвращает ставки.
- Защита от двойных нажатий/ходов в баскетболе.
- Авто‑обновление username в профиле.
- Rate‑limit на команды/кнопки (по умолчанию 1 сек).
- Денежные операции обернуты в транзакции для целостности.
- Магазин с наказаниями, защитами и уникальными титулами.

## Дефолтные параметры (можно менять в `bot/config.py`)
- Стартовый баланс: `1000` `coins`
- Лимиты ставок: `1` – `1_000_000`
- Задержка анимаций: `4.0` сек
- Rate‑limit: `1.0` сек
- 🎰 Слот‑выплаты по `dice.value` (1..64):
  - `>=64` → x5
  - `>=60` → x3
  - `>=48` → x2
  - `>=32` → x1.2
  - ниже → проигрыш
- 🏀 Баскетбол: попадание `value` 4–5, множитель банка `x1.5`
- 🎲 Дуэль: тайм‑аут ожидания `120` сек

## Структура проекта
```
/Users/igorstrizakov/CTFd_analog/
  bot/
    handlers/
    services/
    config.py
    database.py
    keyboards.py
    middlewares.py
    main.py
  data/
  .env.example
  .gitignore
  requirements.txt
  README.md
  CONTEXT.md
```

## Запуск
1. Установить зависимости:
   `pip install -r requirements.txt`
2. Создать `.env` по примеру `.env.example` и прописать `BOT_TOKEN`.
3. Запуск: `python -m bot`

## Примечания
- Для дуэли: `/dice <ставка> <@юзер>` или ответом на сообщение.
- Бот работает асинхронно, результаты после задержки.
